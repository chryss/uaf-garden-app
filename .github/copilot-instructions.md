# Repository instructions

- Do not commit generated seed data such as `firebase/seed-registrations.json`.
- Do not commit files that contain API keys, secrets, or other credentials.
- Prefer local, untracked environment files for machine-specific Firebase config.
- If a placeholder is needed, use a tracked example file instead of real secrets.
